# 变更日志

## 1.0.2 — 2026-09-08

- **修复**：NapCat 等 OneBot 适配器会把通知类事件（`input_status`「正在输入」）转成私聊消息（`type=FRIEND_MESSAGE`、消息链为空、私聊无需唤醒），此前这些通知会给上一条真实消息打取消标记、导致待回复被静默取消；现在只处理真实消息事件（`post_type == message` 且消息链非空）。
- **变更**：白名单改为黑名单 `session_whitelist` → `session_blacklist`（不做旧键兼容）：列表内的会话不启用防抖，留空 = 所有会话启用。
- **变更**：只处理默认会进入 LLM 的消息——命令消息（指令 Handler 直接回复）不再参与防抖，避免命令误取消上一条待回复。

## 1.0.1 — 2026-09-08

- **修复**：合并说明改用 `TextPart` 对象——`extra_user_content_parts` 必须是 ContentPart 实例，此前传 dict 会让核心组装请求时抛 `'dict' object has no attribute 'model_dump_for_context'`，合并请求直接失败、无任何回复。
- **新增**：`session_whitelist` 白名单，条目格式 `G:<群号>` / `F:<用户号>`（群聊与私聊可能同号，必须带前缀，无前缀项忽略并告警）；留空 = 不启用任何会话。
- **变更**：用户维度键加入 G/F 标识，避免群号与用户号相同时群聊与私聊的防抖状态互相串扰。
- **变更**：只有**唤醒 bot 的消息**参与防抖（群聊需 @ 或唤醒前缀，私聊默认可直接唤醒）；未唤醒的消息不记录、不缓冲，也不会取消上一条待发送回复。
- **新增**：`log_with_bot_id` 开关，启用后日志前缀为 `[message_debounce][platform:<平台实例>]`，多 Bot 实例环境可区分来源。
- **新增**：到达记录与窗口等待的 DEBUG 日志（`记录到达` / `等待窗口静默` / `窗口静默结束`），便于定位合并时序。

## 1.0.0 — 2026-09-07

- **新增**：按用户维度（平台实例 + 会话 + 发送者）的纯时间防抖与 prompt 层合并。窗口内新消息取消上一条待发送回复（三道检查点：`on_llm_request` 最早、链尾兜底、`on_llm_response`），被取消的前文合并进最终请求的 `ProviderRequest.prompt`（不改写事件原文，chat_memory 等插件的逐条审计归档不受影响），合并通过事件 extra `message_debounce_merge` 供插件链感知，并向 LLM 追加合并说明（标注 `_no_save` 不入历史）。回复发出即关串，连发链照常合并；bot 自身消息不参与防抖；纯媒体消息计入缓冲。配置为单一 `window`（秒，默认 0，0 = 关闭插件）；纯逻辑 22 项单元测试。
