{
  "window": {
    "type": "float",
    "default": 0,
    "description": "防抖窗口（秒）",
    "hint": "0 = 关闭插件。大于 0 时：同一用户在上一条消息之后的窗口时间内再次发消息，将取消上一条消息的待发送回复、把前文合并进最终请求，只回复最新一条。窗口越大，载体等待与会话锁占用越久（同群其他用户最坏被阻塞一个窗口时长）。"
  }
}
